@model InvoiceViewModel
<head>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
</head>
<div class="container">
    <h1>Pay for Invoice</h1>
    <hr />
    <form action="/invoice/entry" method="post">
        <div class="mb-3">
            <input type="hidden" name="OrderId" value="@Model.OrderId" /> <!--hidden value to update the record -->
            <input type="hidden" name="TableId" value="@Model.TableId" /> <!--hidden value to update the record -->
            <label class="form-label">Invoice No</label>
            <input class="form-control" type="text" name="No" required />
        </div>
        <div class="mb-3">
            <label class="form-label">Order No</label>
            <input class="form-control" type="text" name="OrderNo" value="@Model.OrderNo" required />
        </div>
        <div class="mb-3">
            <label class="form-label">Total Amount</label>
            <input class="form-control" type="number" name="TotalAmount" value="@Model.TotalAmount" required readonly/>
        </div>
        <div class="mb-3">
            <label class="form-label">Payment With</label>
            <select class="form-control" name="PaymentWith">
                <option value="kpay">KPay</option>
                <option value="ayapay">AYAPay</option>
                <option value="cash">Cash</option>
            </select>
        </div>
        <div class="mb-3">
            <label class="form-label">Collector Cashier Employee</label>
            <select class="form-control" name="EmployeeId">
                @foreach (var p in ViewBag.Employees) {
                    <option value="@p.Id">@p.Code :@p.Name</option>
                }
            </select>
        </div>
        <input class="btn btn-success" type="submit" value="Pay">
    </form>
</div>
